# Lumin Team Share: PDF Tools v0.8.6

Use this for an internal Lumin team-channel preview before broader release promotion.

Visual to attach:

- `docs/releases/assets/v0.8.6-lumin-demo.png`

GitHub release, if useful for the team:

- https://github.com/Open-Document-Alliance/PDF-Tools/releases/tag/v0.8.6

## Slack Message

Hey team, quick PDF Tools update. We just hardened `v0.8.6`, and I wanted to share the work before we promote it more broadly.

The big theme: PDF Tools now has a real Sign mode in the MCPB viewer. Before this release, the viewer was mostly view/fill/manage/extract. Now Claude can help identify signature/date zones, the UI exposes a Sign tab, users can draw or reuse a saved signature, and signature/date boxes can be placed onto the PDF.

We also hardened the tricky parts of that experience. Claude is now much better at finding, explaining, and placing signatures/dates on real-world PDFs. The prior internal candidates could detect likely signing areas, but the experience was too easy to get subtly wrong on messy forms: a W-9 signature box could drift toward the printed label, rotated pages could show upright signature overlays on sideways pages, and Claude could have useful coordinates in hidden metadata without those coordinates being visible in the actual response it reasoned from.

This also directly maps to the Sandy Springs case Max raised on April 16: "Download and fill out this PDF [Sandy Springs business license application] ..." The original failure mode was that Claude/Lumin could get stuck before the real PDF work even started: web/proxy fetch issues, no local editable file, unclear fields, then a handoff to the user. In `v0.8.6`, PDF Tools can fetch the PDF URL through the local MCP host, display it, inspect/render pages or regions, identify signature/date zones, and keep the edited working document local for follow-up fill/sign/export steps.

I would describe this as better task performance/reliability from more purpose-built MCP tools, not as raw speed optimization. Instead of asking Claude to solve one giant "download/read/fill/sign" blob, we gave it smaller reliable operations: `fetch_pdf_from_url`, page-bounded reads, text search, page/region rendering, signature-zone detection, and Sign mode.

What changed since the last public release:

- New MCPB viewer Sign mode: Signature and Date zones, Draw signature, saved signatures, manual fields, signing packets, and text stamping.
- Signature/date detection now returns exact visible coordinates in the tool response, so Claude can say where it found a zone and reason from the same data the UI uses.
- W-9 signature/date placement was tuned so the boxes sit on the signing row instead of floating near the labels.
- Rotated-page signing overlays now rotate with the page.
- The signing surface includes inspect-region, region preview, and preview-to-zone flows for PDFs where the automatic detector does not know the right spot.
- Claude now has better PDF reading and visual inspection tools: page-bounded reads, text search, full-page rendering, and region rendering for signatures, stamps, scans, and hard-to-read areas.
- PDF URLs can now be fetched through the local MCP tool instead of relying on sandboxed shell/web fetches.
- Claude Desktop users now get explicit allowed-directory controls for local PDF access.
- Same-document fill/sign flows were hardened so edits preserve the active working file lifecycle instead of multiplying confusing copies.
- CSV bulk-fill handling preserves quoted values with commas.
- XFA form display got a regression pass after `force_xfa=true` fills, including a Claude Desktop sanity check.
- We added a real non-standard municipal PDF test case: the Sandy Springs business-license packet, which has 8 pages and 0 fillable fields but several visible signature/date lines.
- The final `.mcpb` was rebuilt, reinstalled in Claude Desktop, adversarially reviewed, and checked for packaging junk before release.

Evidence sheet attached: it shows the Sandy Springs URL case, the purpose-built tool path, the new Sign mode, and the model-readable coordinates.

Validation snapshot:

- `npm test`: 179 tests passing.
- Release golden placement gate: passing, including the required Sandy Springs URL fixture.
- Claude Desktop reinstall: verified on the final `0.8.6` MCPB.
- W-9, XFA output, and Sandy Springs display/signature-zone checks: verified in Claude Desktop.
- Inspect-region, preview-to-zone, draw-signature, and rotated overlays: verified in browser via Playwright MCP.
- Artifacts verified:
  - MCPB SHA256: `46589af7794174780bec7a036b4c00a0dde49f971824d9681a3e69b4c3c04576`
  - ZIP SHA256: `ad61574c42d5c4446fdd05a216ab1abc33220abe668de3a118c1df612ae9ff55`

One honest caveat: this release solves the PDF substrate for Max's example, not the entire research-agent loop of finding missing answers across bills, contracts, and emails. Product behavior for preview-to-zone is green, but the `agent-browser` smoke harness for that exact flow is still flaky. We validated the behavior with Playwright MCP instead and kept that distinction in the release ledger.

## Shorter Version

PDF Tools `v0.8.6` is the release where the MCPB gets a real Sign mode and a much stronger URL-to-local-PDF path. It directly targets the Sandy Springs case Max raised: fetch a PDF URL locally, inspect/render it, identify Signature and Date zones, keep the working document local, and prepare it for fill/sign/export. Since the last public release it also adds page/region rendering, page-bounded reads, allowed-directory controls, cleaner same-document save behavior, rotated-page signing fixes, XFA display checks, and 179 passing tests. Evidence sheet attached.

## Demo Talking Points

- Start with Max's Sandy Springs prompt: "This was the Lumin-shaped case: fetch a government PDF from a URL, work on it, identify missing data, and request signatures."
- Point at the purpose-built tool path: "The performance improvement is reliability from decomposition: URL fetch, bounded reads/search, page/region rendering, signature-zone detection, then Sign mode."
- Show Sign mode: "This did not exist in the previous public MCPB. Now the viewer has Signature and Date zones plus draw/reuse/manual placement."
- Be clear about scope: "We solved the PDF substrate. The cross-source research part, like finding missing answers in bills/contracts/emails, is a separate agent workflow."
